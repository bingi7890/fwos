{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps --no-audit --prefer-offline",
  "crons": [
    {
      "path": "/api/cron/401k?secret=${CRON_SECRET}",
      "schedule": "0 8 1 * *"
    }
  ]
}
